# NextEdge AI connector demo data

`customers.csv` contains synthetic records only. It can be used for the File Data and Amazon S3 connector smoke tests. The AWS deployment also starts private PostgreSQL, MySQL, and MongoDB services on the application Docker network; their generated credentials stay in AWS Secrets Manager.
