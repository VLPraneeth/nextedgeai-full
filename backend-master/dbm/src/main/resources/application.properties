logging.level.web=INFO
logging.level.org.springframework.data.mongodb.core.MongoTemplate=INFO
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration

spring.data.mongodb.port=27017
spring.data.mongodb.host=localhost
spring.data.mongodb.uri=${mongodb.uri}
spring.data.mongodb.readOnlyUri=${mongodb.readOnlyUri}

spring.cloud.gcp.project-id=${gcp.project}
spring.cloud.gcp.cf.project-id=${gcp.cf.project}
spring.cloud.gcp.credentials.encoded-key=${gcp.credentials.key}
spring.cloud.gcp.profile=development
spring.cloud.gcp.cfdeployer.credentials.encoded-key=${gcp.cfdeployer.credentials.key}
spring.cloud.gcp.cfexecutor.credentials.encoded-key=${gcp.cfexecutor.credentials.key}
spring.cloud.gcp.cfexecutor.sa.email=${gcp.cfexecutor.sa.email}
spring.profiles.active=${spring.profiles.active}

cloud.function.endpoint=${gcp.cloud.functions.url}

event.log.topic.name=${eventlog.topic.name}
event.log.subscription.name=${eventlog.subscription.name}
generic.topic.name=${generic.topic.name}
generic.subscription.name=${generic.subscription.name}
webhook.topic.name=${webhook.topic.name}
webhook.subscription.name=${webhook.subscription.name}
error.notification.topic.name=${error.notification.topic.name}
error.notification.subscription.name=${error.notification.subscription.name}
dfi.result.notification.topic.name=${dfi.result.notification.topic.name}
dfi.result.notification.subscription.name=${dfi.result.notification.subscription.name}
appconfig.api.server.host=${api.server.host}
appconfig.spectrum.server.host=${spectrum.server.host}

notification.pd.email=
mail.smtp.port=587
mail.smtp.host=smtp.gmail.com
mail.sender.username=
mail.sender.from-email=
mail.sender.from-for-error-email=
mail.sender.password=${mail.password}
mail.sender.from-plg-email=${mail.plg.username}
mail.sender.from-plg-password=${mail.plg.password}

google.cloud.cdn.host=${syncari.asset.cdn}
zendesk.shared.secret=${zendesk.shared.secret}
zendesk.sso.secret=${zendesk.sso.secret}
zendesk.support.url=${zendesk.support.url}
zendesk.support.url.alias=${zendesk.support.url.alias}

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB

gcs.bucket.name=${gcs.bucket.name}
gcs.cf.bucket.name=${gcs.cf.bucket.name}

clearbit.api.key=${clearbit.api.key}
syncari.slack.client.id=${syncari.slack.client.id}
syncari.slack.client.secret=${syncari.slack.client.secret}
syncari.datastore.host=${syncari.datastore.host}
syncari.datastore.user=${syncari.datastore.user}
syncari.datastore.password=${syncari.datastore.password}

syncari.salesloft.client.id=${syncari.salesloft.client.id}
syncari.salesloft.client.secret=${syncari.salesloft.client.secret}

environment.name=${environment.name}

workramp.internal.privateKey=${workramp.internal.privateKey}
workramp.internal.certificate=${workramp.internal.certificate}

workramp.academies.privateKey=${workramp.academies.privateKey}
workramp.academies.certificate=${workramp.academies.certificate}

workramp.sp.saml.internal.metadata=${workramp.sp.saml.internal.metadata}
workramp.sp.saml.academies.metadata=${workramp.sp.saml.academies.metadata}

syncari.hubspot.client.id=${syncari.hubspot.client.id}
syncari.hubspot.client.secret=${syncari.hubspot.client.secret}

syncari.gsuite.client.id=${syncari.gsuite.client.id}
syncari.gsuite.client.secret=${syncari.gsuite.client.secret}

spring.redis.host=${redis.host}
spring.redis.port=${redis.port}
spring.redis.user=
spring.redis.password=${redis.password}
spring.redis.timeout=5000

spring.newredis.host=${new.redis.host}
spring.newredis.port=${new.redis.port}
spring.newredis.user=
spring.newredis.password=${new.redis.password}

entitycache.redis.host=${entitycache.redis.db.host}
entitycache.redis.port=${entitycache.redis.db.port}
entitycache.redis.user=
entitycache.redis.password=${entitycache.redis.db.password}

proxy.enabled=${proxy.enabled}
proxy.host=${proxy.host}
proxy.port=${proxy.port}

syncari.jwt.security.secret=${syncari.jwt.security.secret}
syncari.env.name=${syncari.env.name}
webhook.server.host=${webhook.server.host}
webhook.karibu.server.host=${webhook.karibu.server.host}

cf.vpc.connector=${cf.vpc.connector}

